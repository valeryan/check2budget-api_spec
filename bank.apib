## Group Bank

Resources related to banks in the API.

## Banks [/banks/{bank_id}]

A Bank object has the following attributes:

+ name

+ Parameters
    + bank_id (required, number, `1`) ... ID of the Bank in form of an integer

### Retrieve a Bank [GET]

+ Response 200 (application/json)

    + Body

            {
                "errors": false,
                "name": "test bank",
                "description" : "something about this bank",
                "creater_id" : "1"
            }